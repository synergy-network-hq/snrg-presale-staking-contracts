# Conventional EIP-712 staking service configuration is split between:
# - attestor.env (one independent instance per attestor)
# - relayer.env (one durable transaction-submission service)
# - attestor-set.json (the same three addresses configured on-chain)
#
# Deployment inputs and Safe role grants live in ../../config/production.env.example.
